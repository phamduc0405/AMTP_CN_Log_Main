<UserControl
    x:Class="MTP_MANUAL_App.Style.ButtonsLine"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="150"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />

        </Grid.ColumnDefinitions>

        <Grid
            Grid.Row="0"
            Grid.Column="0"
            Margin="5,0,0,0">
            <Button
                x:Name="btnNb1"
                Content="button"
                Style="{StaticResource ButtonLineUnder}" />
        </Grid>
        <Grid
            Grid.Row="0"
            Grid.Column="1"
            Margin="5,0,0,0">
            <Button
                x:Name="btnNb2"
                Content="button"
                Style="{StaticResource ButtonLineUnder}" />
        </Grid>
        <Grid
            Grid.Row="0"
            Grid.Column="2"
            Margin="5,0,0,0">
            <Button
                x:Name="btnNb3"
                Content="button"
                Style="{StaticResource ButtonLineUnder}" />
        </Grid>
    </Grid>
</UserControl>
